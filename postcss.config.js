// Tailwind v4+ uses the @tailwindcss/postcss plugin name, but for utility compatibility, map to default export.
// PostCSS config (Tailwind v3)
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
